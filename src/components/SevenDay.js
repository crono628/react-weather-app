import React from 'react';
import DailyCard from './DailyCard';

const SevenDay = (props) => {
  // const handleSevenDays = (i) => {
  //   for (let i = 0; i < 7; i++) {
  //     return (
  //       <DailyCard
  //         date={(i) => props.date}
  //         icon={(i) => props.icon}
  //         low={(i) => props.low}
  //         high={(i) => props.high}
  //       />
  //     );
  //   }
  // };
  // return <div>{handleSevenDays(i)}</div>;
};

export default SevenDay;
